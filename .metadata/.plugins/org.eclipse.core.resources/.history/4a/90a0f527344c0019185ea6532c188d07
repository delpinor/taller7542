#include "View.h"

Model * viewmodel;

        View::View(Model * modelo) {
           viewmodel = modelo;
        }
        View::View() {}
        void View::SetModel(Model * modelo) {
            viewmodel = modelo;
        }
        void View::Render() {

        }
